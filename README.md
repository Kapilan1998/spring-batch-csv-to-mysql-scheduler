# spring-batch-csv-to-mysql-scheduler
A Spring Boot project that uses Spring Batch and Spring Scheduler to automatically read data from a local CSV file every 2 minutes and store it into a MySQL database.
